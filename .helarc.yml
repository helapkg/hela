tasks:
  format: prettier-eslint --write ./{test,src}/**/*.{mjs,js} --config ./.prettierrc.json

  lint: eslint ./{test,src}/**/*.{mjs,js} --config ./.eslintrc.json --format codeframe --fix

  test:
    - rollup -c
    - nyc --reporter=lcov node ./dist/test.js
    - nyc report
    - nyc check-coverage

  style:
    - yarn start format
    - yarn start lint

  precommit:
    - git status --porcelain
    - yarn start test
    - git add --all

  commit:
    - simple-commit-message

  release:
    - semantic-release pre
    - npm publish
    - semantic-release post
